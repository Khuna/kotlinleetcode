package todo

class MinimumPathSum {

    fun test() {
        var grid = arrayOf(
            intArrayOf(1,3,1),
            intArrayOf(1,5,1),
            intArrayOf(4,2,1)
        )

        println(minPathSum(grid))
    }

    fun minPathSum(grid: Array<IntArray>): Int {

        
        return 0
    }
}